database: schemahero
name: users
requires: []
schema:
  mysql:
    primaryKey: [id]
    columns:
      - name: id
        type: integer
      - name: email
        type: varchar(255)
        constraints:
          notNull: true
      - name: account_type
        type: varchar(10)
      - name: num_seats
        type: integer
