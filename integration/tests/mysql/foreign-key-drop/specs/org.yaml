database: schemahero
name: org
schema:
  mysql:
    primaryKey:
    - id
    foreignKeys:
    columns:
    - name: id
      type: integer
      constraints:
        notNull: true
    - name: project_id
      type: integer
      constraints:
        notNull: true
