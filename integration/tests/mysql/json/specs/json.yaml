database: schemahero
name: complex_data
requires: []
schema:
  mysql:
    primaryKey:
      - id
    columns:
      - name: id
        type: integer
      - name: complex_data
        type: json
        constraints:
          notNull: true
