database: schemahero
name: user_projects
schema:
  mysql:
    columns:
      - name: user_id
        type: int
        constraints:
          notNull: true
      - name: project_id
        type: int
        constraints:
          notNull: true
