database: schemahero
name: users
schema:
  mysql:
    primaryKey: [id]
    columns:
      - name: id
        type: integer
        constraints:
          notNull: true
        attributes:
          autoIncrement: true
      - name: name
        type: varchar(255)
        constraints:
          notNull: true
