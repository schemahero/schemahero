apiVersion: schemas.schemahero.io/v1alpha2
kind: Table
metadata:
  name: other
spec:
  database: schemahero
  name: other
  schema:
    mysql:
      primaryKey:
      - id
      columns:
      - name: id
        type: int
        constraints:
          notNull: true
      - name: something
        type: varchar (255)
        constraints:
          notNull: true
