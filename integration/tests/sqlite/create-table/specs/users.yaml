database: schemahero
name: users
requires: []
schema:
  sqlite:
    primaryKey: [id]
    columns:
      - name: id
        type: integer
      - name: login
        type: varchar(255)
      - name: name
        type: varchar(255)
        constraints:
          notNull: true
        default: ethan
