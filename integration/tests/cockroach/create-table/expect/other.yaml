apiVersion: schemas.schemahero.io/v1alpha2
kind: Table
metadata:
  name: other
spec:
  database: schemahero
  name: other
  schema:
    postgres:
      primaryKey:
      - id
      columns:
      - name: id
        type: integer
        constraints:
          notNull: true
      - name: something
        type: character varying (255)
        constraints:
          notNull: true
