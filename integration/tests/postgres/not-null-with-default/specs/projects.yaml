database: schemahero
name: projects
schema:
  postgres:
    primaryKey: [id]
    columns:
      - name: id
        type: integer
        constraints:
          notNull: true
      - name: name
        type: varchar(255)
        default: "unnamed"
        constraints:
          notNull: true
      - name: icon_uri
        type: varchar(255)

