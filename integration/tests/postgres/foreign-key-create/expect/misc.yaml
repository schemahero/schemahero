apiVersion: schemas.schemahero.io/v1alpha1
kind: Table
metadata:
  name: misc
spec:
  database: schemahero
  name: misc
  schema:
    postgres:
      primaryKey:
      - pk
      columns:
      - name: pk
        type: character varying (255)
        constraints:
          notNull: true
