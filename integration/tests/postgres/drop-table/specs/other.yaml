database: schemahero
name: other
requires: []
schema:
  postgres:
    isDeleted: true
    primaryKey: [id]
    columns:
      - name: id
        type: integer
      - name: something
        type: varchar(255)
        constraints:
          notNull: true
