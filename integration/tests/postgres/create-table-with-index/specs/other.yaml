database: schemahero
name: other
requires: []
schema:
  postgres:
    primaryKey: [id]
    indexes:
      - columns: [something]
        isUnique: false
    columns:
      - name: id
        type: integer
      - name: something
        type: varchar(255)
        constraints:
          notNull: true
