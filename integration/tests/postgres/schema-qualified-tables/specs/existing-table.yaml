database: schemahero
name: existing_table
requires: []
schema:
  postgres:
    schema: myschema
    primaryKey: [id]
    indexes:
      - columns: [name]
        name: idx_existing_table_name
    columns:
      - name: id
        type: integer
      - name: name
        type: varchar(255)
        constraints:
          notNull: true
