database: schemahero
name: users
requires: []
schema:
  cassandra:
    primaryKey:
      - [id]
    columns:
      - name: id
        type: int
      - name: login
        type: varchar
      - name: name
        type: varchar
        default: ethan
