database: schemahero
name: other
requires: []
schema:
  rqlite:
    primaryKey: [id]
    columns:
      - name: id
        type: integer
        constraints:
          notNull: true
      - name: something
        type: text
        constraints:
          notNull: true
