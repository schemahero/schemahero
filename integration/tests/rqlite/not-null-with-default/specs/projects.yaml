database: schemahero
name: projects
schema:
  rqlite:
    primaryKey: [id]
    columns:
      - name: id
        type: integer
        constraints:
          notNull: true
      - name: name
        type: text
        default: "unnamed"
        constraints:
          notNull: true
      - name: icon_uri
        type: text

