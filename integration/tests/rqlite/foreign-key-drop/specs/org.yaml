database: schemahero
name: org
schema:
  rqlite:
    primaryKey: [id]
    columns:
    - name: id
      type: integer
      constraints:
        notNull: true
    - name: project_id
      type: integer
