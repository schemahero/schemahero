database: schemahero
name: org
schema:
  timescale:
    primaryKey:
    - id
    columns:
    - name: id
      type: integer
      constraints:
        notNull: true
    - name: project_id
      type: integer
      constraints:
        notNull: true
