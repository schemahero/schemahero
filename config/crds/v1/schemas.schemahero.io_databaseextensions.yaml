apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: databaseextensions.schemas.schemahero.io
spec:
  group: schemas.schemahero.io
  names:
    kind: DatabaseExtension
    listKind: DatabaseExtensionList
    plural: databaseextensions
    singular: databaseextension
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.database
      name: Database
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha4
    schema:
      openAPIV3Schema:
        description: DatabaseExtension is the Schema for the databaseextensions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseExtensionSpec defines the desired state of DatabaseExtension
            properties:
              database:
                description: Database is the name of the database where this extension
                  should be created
                type: string
              postgres:
                description: Postgres contains PostgreSQL-specific extension configuration
                properties:
                  name:
                    description: Name is the name of the extension to create
                    type: string
                  schema:
                    description: Schema is the optional schema to create the extension
                      in
                    type: string
                  version:
                    description: Version is the optional version of the extension
                    type: string
                required:
                - name
                type: object
            required:
            - database
            type: object
          status:
            description: DatabaseExtensionStatus defines the observed state of DatabaseExtension
            properties:
              appliedAt:
                description: AppliedAt is the timestamp when the extension was last
                  applied
                format: int64
                type: integer
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              phase:
                description: Phase represents the current phase of the extension (Pending,
                  Applied, Failed)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
